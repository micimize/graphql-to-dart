{{~#*inline "directivesBlock" }}
{{#if values.length}}

{{#each values}}
{{../directive}} '{{this}}';
{{/each}}
{{/if}}
{{/inline~}}

// GENERATED CODE - DO NOT MODIFY BY HAND
{{> directivesBlock values=@root.config.documents.imports directive='import'}}
{{> directivesBlock values=@root.config.documents.exports directive='export'}}
{{> directivesBlock values=@root.config.documents.parts directive='part'}}

part '
{{~#if operations.length ~}}
    {{~ expectedGeneratedFileFor operations ~}}
{{!--~else~}} adding the json serializers to mixins doesn't really make sense.
    {{~ expectedGeneratedFileFor fragments ~--}}
{{~/if~}}';

{{#each fragments }}
{{> fragment }}
{{/each}}

{{#each operations}}
{{> operation }}
{{/each}}


class {{ className }} {{ classExtends interfaces }} {
  {{#each this}}{{#if type}}{{#if name}}
  {{#if description }}{{ multilineComment description }}
  {{/if}}final {{ resolveType type ../contextName ../contextModels }} {{name}};
  {{~/if}}{{/if}}{{/each}}

  {{className}}({
    {{#each this}}{{#if name}}
    {{~#if isRequired}}@required {{/if}}this.{{name}},
    {{/if}}{{/each}}
  });
}

@GenSerializer()
class {{ className }}Serializer extends Serializer<{{ className }}> with _${{ className }}Serializer {
}

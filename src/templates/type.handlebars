{{ multilineComment description }}
{{> defineClass fields className=name }}

{{#each fields}}{{# if hasArguments }}
{{> defineClass arguments className=(concat ../name (toPascalCase name) "Args") }}
{{/if}}{{/each}}
